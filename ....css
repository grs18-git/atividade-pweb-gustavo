:root{
  --pitch: #0b3d2e;
  --pitch-deep: #062820;
  --pitch-light: #14543f;
  --chalk: #f2f5ef;
  --chalk-dim: #c9d2c6;
  --floodlight: #ffd23f;
  --floodlight-dim: #a8862c;
  --ink: #08211a;
  --erro: #ff6b5e;

  --fonte-display: 'Anton', sans-serif;
  --fonte-corpo: 'Inter', sans-serif;
  --fonte-dado: 'IBM Plex Mono', monospace;
}

*{ box-sizing: border-box; }

body{
  margin: 0;
  min-height: 100vh;
  font-family: var(--fonte-corpo);
  color: var(--chalk);
  background:
    radial-gradient(ellipse 900px 500px at 50% -10%, rgba(255,210,63,0.10), transparent 60%),
    repeating-linear-gradient(
      90deg,
      var(--pitch) 0px,
      var(--pitch) 80px,
      var(--pitch-light) 80px,
      var(--pitch-light) 160px
    );
  display: flex;
  justify-content: center;
  padding: 56px 20px 80px;
}

.campo{
  width: 100%;
  max-width: 640px;
}

/* ---------- Cabeçalho ---------- */

.topo{ text-align: center; margin-bottom: 34px; }

.apito{
  font-family: var(--fonte-dado);
  font-size: 12px;
  letter-spacing: 3px;
  text-transform: uppercase;
  color: var(--floodlight);
  margin: 0 0 10px;
}

.titulo{
  font-family: var(--fonte-display);
  font-weight: 400;
  font-size: clamp(40px, 8vw, 64px);
  letter-spacing: 1px;
  margin: 0 0 12px;
  color: var(--chalk);
  text-shadow: 0 0 24px rgba(255,210,63,0.25);
}

.legenda{
  font-size: 14.5px;
  color: var(--chalk-dim);
  max-width: 420px;
  margin: 0 auto;
  line-height: 1.6;
}

/* ---------- Formulário / catraca ---------- */

.catraca{
  display: flex;
  gap: 10px;
  background: var(--pitch-deep);
  border: 1px solid rgba(255,255,255,0.08);
  border-radius: 10px;
  padding: 8px;
  box-shadow: 0 20px 40px rgba(0,0,0,0.35), inset 0 0 0 1px rgba(255,255,255,0.02);
}

.campo-texto{
  flex: 1;
  border: none;
  background: transparent;
  color: var(--chalk);
  font-family: var(--fonte-corpo);
  font-size: 15.5px;
  padding: 14px 16px;
  outline: none;
}
.campo-texto::placeholder{ color: rgba(242,245,239,0.35); }

.botao-apitar{
  border: none;
  border-radius: 6px;
  background: var(--floodlight);
  color: var(--ink);
  font-family: var(--fonte-corpo);
  font-weight: 700;
  font-size: 14px;
  letter-spacing: 0.3px;
  padding: 0 22px;
  cursor: pointer;
  transition: transform 0.15s ease, background 0.15s ease;
}
.botao-apitar:hover{ background: #ffdd66; transform: translateY(-1px); }
.botao-apitar:active{ transform: translateY(0); }
.botao-apitar:focus-visible,
.campo-texto:focus-visible{
  outline: 2px solid var(--floodlight);
  outline-offset: 2px;
}

/* ---------- Mensagens de status ---------- */

.mensagem-status{
  text-align: center;
  font-size: 13.5px;
  font-family: var(--fonte-dado);
  min-height: 20px;
  margin: 16px 0 0;
  color: var(--chalk-dim);
}
.mensagem-status.erro{ color: var(--erro); }

/* ---------- Resultado: TICKET DE ESTÁDIO ---------- */

.area-resultado{ margin-top: 30px; }

.ticket{
  position: relative;
  display: grid;
  grid-template-columns: 1fr auto;
  background: var(--chalk);
  color: var(--ink);
  border-radius: 14px;
  overflow: hidden;
  box-shadow: 0 30px 60px rgba(0,0,0,0.4);
  animation: entrada-ticket 0.45s ease both;
}

@keyframes entrada-ticket{
  from{ opacity: 0; transform: translateY(14px); }
  to{ opacity: 1; transform: translateY(0); }
}

.ticket-principal{
  padding: 28px 26px;
}

.ticket-liga{
  font-family: var(--fonte-dado);
  font-size: 11px;
  letter-spacing: 2px;
  text-transform: uppercase;
  color: var(--floodlight-dim);
  margin: 0 0 8px;
}

.ticket-nome{
  font-family: var(--fonte-display);
  font-size: clamp(26px, 5vw, 34px);
  font-weight: 400;
  line-height: 1.05;
  margin: 0 0 18px;
  color: var(--ink);
}

.ticket-linha{
  display: flex;
  justify-content: space-between;
  align-items: baseline;
  padding: 9px 0;
  border-top: 1px dashed rgba(8,33,26,0.15);
  gap: 12px;
}
.ticket-linha:first-of-type{ border-top: none; }

.ticket-rotulo{
  font-size: 11px;
  letter-spacing: 1.5px;
  text-transform: uppercase;
  color: #5c6b62;
  white-space: nowrap;
}

.ticket-valor{
  font-family: var(--fonte-dado);
  font-size: 14.5px;
  font-weight: 600;
  text-align: right;
}

.ticket-sinopse{
  margin-top: 16px;
  padding-top: 14px;
  border-top: 1px dashed rgba(8,33,26,0.15);
  font-size: 13px;
  line-height: 1.6;
  color: #3a463f;
}

.ticket-redes{
  margin-top: 16px;
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
}
.ticket-redes a{
  font-family: var(--fonte-dado);
  font-size: 11.5px;
  text-decoration: none;
  color: var(--ink);
  background: rgba(8,33,26,0.06);
  border: 1px solid rgba(8,33,26,0.12);
  border-radius: 20px;
  padding: 5px 12px;
  transition: background 0.15s ease;
}
.ticket-redes a:hover{ background: var(--floodlight); }

/* canhoto do ticket, com o escudo */

.ticket-canhoto{
  position: relative;
  width: 148px;
  background: var(--pitch-deep);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 10px;
  padding: 20px 14px;
}

.ticket-canhoto::before{
  content: "";
  position: absolute;
  inset: 0;
  left: 0;
  width: 0;
  border-left: 2px dashed rgba(242,245,239,0.25);
}

/* recortes circulares estilo ticket de ingresso */
.ticket::after{
  content: "";
  position: absolute;
  top: 0; bottom: 0;
  right: 148px;
  width: 0;
}

.entalhe{
  position: absolute;
  width: 22px;
  height: 22px;
  background: var(--pitch-deep);
  border-radius: 50%;
  right: 137px;
}
.entalhe.topo{ top: -11px; }
.entalhe.base{ bottom: -11px; }

.escudo{
  width: 76px;
  height: 76px;
  object-fit: contain;
  filter: drop-shadow(0 6px 12px rgba(0,0,0,0.35));
}

.escudo-ausente{
  width: 76px;
  height: 76px;
  border-radius: 50%;
  border: 2px dashed rgba(242,245,239,0.3);
  display: flex;
  align-items: center;
  justify-content: center;
  font-family: var(--fonte-display);
  font-size: 26px;
  color: rgba(242,245,239,0.4);
}

.ticket-selo{
  font-family: var(--fonte-dado);
  font-size: 10px;
  letter-spacing: 1.5px;
  text-transform: uppercase;
  color: var(--floodlight);
  text-align: center;
}

/* ---------- Responsivo ---------- */

@media (max-width: 480px){
  .ticket{ grid-template-columns: 1fr; }
  .ticket-canhoto{
    flex-direction: row;
    width: auto;
    padding: 16px 20px;
    justify-content: flex-start;
  }
  .ticket-canhoto::before{ display: none; }
  .entalhe{ display: none; }
  .ticket-linha{ flex-direction: column; align-items: flex-start; gap: 2px; }
  .ticket-valor{ text-align: left; }
}

/* respeita usuários que preferem menos movimento */
@media (prefers-reduced-motion: reduce){
  .ticket{ animation: none; }
}
